# ELMO 單字卡

這是一個純前端的背單字應用程式，使用 HTML、CSS、JavaScript 實作，並支援：

- 中央卡片點擊翻頁
- 正面顯示英文單字
- 背面顯示翻譯、詞性、例句、字根分析
- 管理頁面可新增、編輯、刪除單字
- 自動填入按鈕會呼叫公開 API 自動補入資料

## 使用方式

1. 開啟 `index.html`。
2. 在「管理頁」新增單字資料。
3. 按下「自動填入」可嘗試自動填補翻譯、詞性、例句、字根分析。
4. 在「學習頁」點擊卡片翻面，使用上下頁切換單字。
